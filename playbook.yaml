---
# - name: Generate files
#   hosts: localhost
#   roles:
#     - base

- name: Distribute files to Controllers
  hosts: controllers
  roles:
    - controllers


# - name: Generate kubeconfig
#   hosts: localhost
#   tasks:
#     - name: Run generate_kubeconfig.sh
#       shell: /home/apostoliseq/projects/kubernetes-the-hard-way/generate_kubeconfig.sh

# - name: Generate kubeconfig
#   hosts: localhost
#   tasks:
#     - name: Run generate_kubeconfig.sh
#       shell: /home/apostoliseq/projects/kubernetes-the-hard-way/generate_kubeconfig.sh